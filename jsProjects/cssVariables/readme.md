instead of using 'change' even listner to get any changes in input fields
use 'input' even listner since it listens to every short changes, whereas 'change' only listens when we are done giving input
